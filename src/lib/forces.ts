export type simpleForceSettings = {
  linkDistance: number
  linkStrength: number
  chargeStrength: number
}

export const distantLayoutForce = {
  linkDistance: 30,
  linkStrength: 0.2,
  chargeStrength: -40,
}
